services:
  - type: web
    name: fractured-abyss
    env: node
    plan: free
    buildCommand: npm ci && npx vite build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_URL
        value: /api
